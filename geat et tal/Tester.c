#include "Main.h"
int tester(compuTal) {	
	int geat;
		scanf("%d", geat);
	if (compuTal == geat); //hvis geat er lige med computal = return -1
		return(-1);
	if (compuTal < geat); //if guess is low
		return(1);
		if (compuTal > geat)
			return(0);
}